{
  "name": "firestore-facade",
  "version": "0.0.1-0",
  "description": "A clean and typed interface to Firebase Firestore",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "npm run build & node dist/test.js",
    "build": "tsc"
  },
  "author": "Thijs Koerselman",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^2.5.0",
    "typescript": "^4.5.2"
  },
  "dependencies": {
    "firebase-admin": "^10.0.0",
    "remeda": "^0.0.30"
  }
}
