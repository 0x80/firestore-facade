{
  "name": "firestore-facade-packages",
  "version": "0.0.0",
  "private": true,
  "description": "A monorepo for firestore-facade related packages",
  "author": "Thijs Koerselman",
  "license": "MIT",
  "workspaces": [
    "packages/cli",
    "packages/facade",
    "packages/example-nodejs"
  ],
  "scripts": {
    "clean": "tsc --build --clean",
    "build": "tsc --build",
    "watch": "tsc --build --watch"
  },
  "devDependencies": {
    "typescript": "^4.5.2"
  }
}
